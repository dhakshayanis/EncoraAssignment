describe('one',function()
{

   it('write Json',function()
   {
    let output = "";

    for(var alphabet = 'a'; alphabet <='z'; alphabet++ )
        {
            //output += alphabet;
            cy.log(alphabet)
        }
   })
})
